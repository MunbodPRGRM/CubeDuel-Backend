/**
 * Seed ข้อมูลทดสอบ — ผู้ใช้ 10 คน + Rating 4 แถวต่อคน (elo_rating = 1000)
 * TODO(เฟส 1 รอบเต็ม): เขียนจริงหลัง schema ครบ 12 ตาราง
 */
async function main() {
  console.log('[seed] ยังไม่ได้เขียน — รอ schema ครบ 12 ตารางก่อน');
}

main();
